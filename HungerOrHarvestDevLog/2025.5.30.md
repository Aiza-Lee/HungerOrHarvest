# 2025.5.30



## 项目进展

* 修复上次修改`enum`序列化方法后产生的不能正常显示村民card的问题
    * 问题在于当时判定`group`类型的时候直接判断了`archType`是不是`ArchType.None`（现在被`Nullable`取缔），导致所有的非建筑类型的`tag`在被点击后都会显示`ArchType`中默认值为`0`的那种建筑的相关信息

* 为`groupLayoutBase`添加排序接口，方便实现元素重排序
* 修复 `SmoothChangeBase` 在正常结束并触发回调后没有清空回调的问题



## 遇到的问题

1. UI的逻辑愈发混乱，尤其是在清空、初始化的时候，打算从头设计一下

