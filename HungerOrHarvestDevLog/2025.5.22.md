# 2025.5.22



## 项目进展

* 在某些未调用的接口实现中添加`#pragma warning disable 67`消除警告

* 发现问题：在当前世界上加载新世界的逻辑有问题，导致前面世界的东西没删干净
    * 修复：在`WorldMgr`的`ClearMgr`方法中依次调用`Vill`和`Arch`的`Destroy()`，因为列表/字典中删除的操作由因为监听了销毁事件（毕竟还有别的操作会销毁这两种对象），所以不能用迭代器枚举对象