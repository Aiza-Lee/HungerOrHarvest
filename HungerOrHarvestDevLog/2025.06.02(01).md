# 2025.05.02(01)



## 项目进展

* 简化`RepoMgr`的添加，消耗资源的实现和调用
* 部分地方用`Linq`重写，太伟大了`Linq`
* 完成`VillLogic`的结构重构，实现方法和下面的计划中说明的相同
* 发现通过`Coord`检测周围可以走的`OL`的时候的bug，如果在`y`方向的路上的话，还需要额外判断当前所在路径这一小段的上下是否有建筑，从而避免村民从建筑后方传到建筑前方



## 计划

* 改变`villLogic`结构，整一个代表村民的接口暴露必要的`API`，然后在内部搞一个`LogicImpler`，再在`Impler`里面搞之前的一堆`helper`的通信问题，这样就可以既有内部的多`public`必要的紧耦合的实现，又可以在外部只暴露必要的`API`