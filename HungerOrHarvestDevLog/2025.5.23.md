# 2025.5.23



## 项目进展

* 解决先前修改`enum`内容会导致根据整型存储的SO对象的枚举字段错位。具体解决方法是配置文件中**使用字符串表示**
    * 同时修改所有存储文件的逻辑
    * 修改 `WorldGenerator`配置形式
    * 不直接序列化 `RTList` 和 `JTList` 对象， 而是采用 `ISaveable` 逻辑
* 调整配置文件逻辑，把先前的 `ConstMgr` 分为新的 `ConstMgr` 和 `ConfigMgr`  ，分别管理保证逻辑正常的常量和配置文件（之前把配置文件在字面上解释为常量，确实不好）

* 删掉枚举中的None，全部改为 `Nullable` 结构体实现



## 遇到的问题

* 不知道改了啥，村民面板不显示村民了，，，