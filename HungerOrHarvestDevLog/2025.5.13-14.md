

# 2025.05.13-14



## 项目进展

* 为没有配置`SmoothMove`的`Config`的`component`添加默认的线性配置，之前是在脚本为物体挂载该`component`的时候设置，现在直接在`SmoothMove`的`Awake()`方法中调用
* 优化`RepoMgr`计算逻辑，代码更清晰，效率略低

* 为 `RepoMgr`添加资源产出的统计（每日总额，上一秒（队列统计逻辑帧的产出）），顺带实现每日结算面板，还没写具体的内容
* 为`RepoMgr`添加注释
* **发现致命bug，深拷贝 `RTList` 的时候没有深拷贝底层的 `RTPair` ！！**
* 添加 `README.md` 文件



## 遇到的问题

* 发现通过`UIMgr`管理的UI只能作为单例存在（毕竟是通过类来查找panel的），这导致同一个panel一定不会出现多个，这对于 Popup 用途的 panel 来说似乎有点问题，目前的解决方案就是从 Popup Panel 基类派生出子类，放弃对同一个 Panel 同时出现的支持



## 计划

* 添加删除存档功能